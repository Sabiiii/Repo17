const _ = require('lodash');



function findUserById(id) {
    try {

    } catch (error) {
        return 
        console.log();
    }
}

getUsers();
findUserById();

module.exports = findUserById;